<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed State -->
    <item android:state_pressed="true">
        <layer-list>
            <item>
                <!-- Shadow -->
                <shape android:shape="rectangle">
                    <solid android:color="@color/shadow_medium" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
            <item android:top="2dp" android:left="2dp" android:right="2dp" android:bottom="6dp">
                <!-- Button Background -->
                <shape android:shape="rectangle">
                    <solid android:color="@color/primary_purple_dark_new" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
        </layer-list>
    </item>
    
    <!-- Disabled State -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/text_tertiary_light_gray" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    
    <!-- Default State -->
    <item>
        <layer-list>
            <item>
                <!-- Shadow -->
                <shape android:shape="rectangle">
                    <solid android:color="@color/shadow_light" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
            <item android:top="0dp" android:left="0dp" android:right="0dp" android:bottom="4dp">
                <!-- Button Background -->
                <shape android:shape="rectangle">
                    <solid android:color="@color/primary_blue" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector> 