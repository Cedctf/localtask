<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Main content layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Fragment Container -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Bottom Navigation -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/background_white"
            android:elevation="8dp"
            app:menu="@menu/bottom_nav_menu"
            app:itemIconTint="@color/text_secondary_gray"
            app:itemTextColor="@color/text_secondary_gray"
            app:itemIconSize="24dp"
            app:labelVisibilityMode="auto"/>

    </LinearLayout>

    <!-- Create Task FAB - Floating above chatbot (only for hirers) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_create_task"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="160dp"
        android:src="@android:drawable/ic_input_add"
        android:contentDescription="Create Task"
        android:visibility="gone"
        app:tint="@color/text_white"
        app:backgroundTint="@color/primary_purple"
        app:elevation="6dp"
        app:pressedTranslationZ="12dp" />

    <!-- Chatbot FAB - Floating over all content -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_chatbot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="88dp"
        android:src="@android:drawable/ic_dialog_email"
        android:contentDescription="AI Assistant"
        app:tint="@color/text_white"
        app:backgroundTint="@color/primary_purple"
        app:elevation="6dp"
        app:pressedTranslationZ="12dp" />

</FrameLayout> 